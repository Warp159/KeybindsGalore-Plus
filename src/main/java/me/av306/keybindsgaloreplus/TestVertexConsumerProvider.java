package me.av306.keybindsgaloreplus;

import net.minecraft.client.render.RenderLayer;
import net.minecraft.client.render.VertexConsumerProvider;
import net.minecraft.client.util.BufferAllocator;

//import java.util.SequencedMap;

public class TestVertexConsumerProvider //extends VertexConsumerProvider.Immediate
{
    /*protected TestVertexConsumerProvider( BufferAllocator allocator, SequencedMap<RenderLayer, BufferAllocator> sequencedMap )
    {
        super( allocator, sequencedMap );
    }

    @Override
    public void draw( RenderLayer layer )
    {
        super.draw( layer );
    }*/
}
